{% extends '@Daniel/baseLayout.html.twig' %}
{% block main %}
    <div id="picture-{{ image.id }}">
        <h1>{{ image.title }}</h1>
        <img src="{{ asset('uploads/images/' ~ image.image ) }}"  width="900" height="600"  alt="{{ image.title }}"/>

        {% if image.description %}
            <p>{{ image.description }}</p>
        {% endif %}
    </div>

    <div class="control">
        <a href="{{ path('image_detail',  { 'id': before.id, 'slug': before.title|slugify }) }}" class="btn">
            <i class="fa fa-caret-left"></i> Älterer Eintrag</a>

        <a href="{{ path('image_overview') }}">Overview</a>
    </div>
{% endblock %}